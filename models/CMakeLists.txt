cmake_minimum_required(VERSION 2.6)
project(sensor_models)
add_subdirectory(LucidControl)
add_subdirectory(Simulator)
add_subdirectory(ZBSensor)
IF (BASLER)
  MESSAGE(STATUS "Adding Basler Support")
  add_subdirectory(cameras/Basler)
  FILE(REMOVE cameras/Basler/UnitServerIgnore)
ELSE()
  FILE(WRITE cameras/Basler/UnitServerIgnore "disabled ${PROJECT_NAME}")
ENDIF()
SET(DRIVERS_LIB ${DRIVERS_LIB} PARENT_SCOPE)
